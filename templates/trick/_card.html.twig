<div class="col-12 col-sm-6 col-md-4 col-lg-3 d-flex align-items-stretch">
	<div class="card mb-4">
		<a href="{{ path('trick_show', {'group_slug': t.trickGroup.slug, 'slug': t.slug}) }}"><img src="{{ asset('img/visit-almaty-wN4D-mVR7fE-unsplash.jpg') }}" class="img-fluid" alt="Image de {{ t.name }}"></a>
		<div class="card-body">
			<span class="badge badge-info badge-pill">
				{{ t.trickGroup.name }}
			</span>
			<h4 class="card-title">
				<a href="{{ path('trick_show', {'group_slug': t.trickGroup.slug, 'slug': t.slug}) }}">{{ t.name }}</a>
			</h4>
		</div>
	</div>
</div>
