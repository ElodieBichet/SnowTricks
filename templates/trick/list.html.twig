{% for trick in tricks %}
	{{ include('trick/_card.html.twig') }}
{% endfor %}

<script>
	$(document).ready(function () {
{% if isLastPage == false %}
$("#loadmore").attr('href', "{{ path('trick_page_with_limit', {page: tricks.getCurrentPageNumber+1, limit: tricks.getItemNumberPerPage}) }}");
{% else %}
$('#loadmore').remove();{% endif %}
});
</script>
