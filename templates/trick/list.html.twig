{% for trick in tricks %}
	{{ include('trick/_card.html.twig') }}
{% endfor %}

{% if isLastPage == false %}
	<span class="d-none">
		{{ path('trick_page_with_limit', {page: tricks.getCurrentPageNumber+1, limit: tricks.getItemNumberPerPage}) }}
	</span>
{% else %}
	<span class="d-none"></span>
{% endif %}
