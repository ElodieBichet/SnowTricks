{{ form_start(form, {'attr': {'id': 'picture'}}) }}
{{ form_widget(form) }}
<button class="btn btn-primary h-100 me-1 mt-1">
	<i class="fas fa-save"></i>
	{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
